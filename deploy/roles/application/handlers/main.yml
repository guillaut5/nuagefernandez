- name: daemon-reload
  systemd:
    daemon_reload: yes

- name: restart app
  systemd:
    name: "{{ service_name }}"
    state: restarted
    enabled: yes

- name: reload nginx
  systemd:
    name: nginx
    state: reloaded
